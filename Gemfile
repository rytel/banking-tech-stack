source "https://rubygems.org"

# fastlane drives every build and test entry point for this repo — see
# `fastlane/Fastfile`. Pinned through Gemfile.lock, which is committed so a
# CI runner resolves exactly the same versions as a local machine.
gem "fastlane"
